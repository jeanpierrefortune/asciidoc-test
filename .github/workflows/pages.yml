name: Deploy AsciiDoc to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Asciidoctor
        run: sudo gem install asciidoctor rouge asciidoctor-kroki asciidoctor-pdf prawn-svg

      - name: Install Montserrat fonts
        run: |
          sudo apt-get install -y fonts-montserrat
          mkdir -p fonts
          find /usr/share/fonts -name "Montserrat-*.ttf" -exec cp {} fonts/ \;
          ls -lh fonts/

      - name: Convert logo SVG to PNG
        run: |
          sudo apt-get install -y librsvg2-bin
          rsvg-convert -f png -w 300 logo-calypso.svg -o logo-calypso.png

      - name: Convert AsciiDoc to HTML
        run: |
          asciidoctor api_specification.adoc \
            --out-file index.html \
            -a rouge-style=github \
            -a docinfo=shared

      - name: Convert AsciiDoc to PDF
        run: |
          asciidoctor-pdf api_specification.adoc \
            --out-file spec.pdf \
            -a pdf-theme=calypso \
            -a pdf-themesdir="$(pwd)" \
            -a pdf-fontsdir="$(pwd)/fonts;GEM_FONTS_DIR" \
            -a imagesdir="$(pwd)" \
            -a allow-uri-read \
            -a revdate="$(date +'%Y-%m-%d')"

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
